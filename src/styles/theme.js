import { extendTheme } from '@chakra-ui/react'

const theme = extendTheme({
  colors: {
    primary: {
      500: '#001C30',
    },
    lightPrimary: {
      500: '#176B87',
    },
    secondary: {
      500: '#64CCC5',
    },
    lightSecondary: {
      500: '#DAFFFB',
    },
  },
})

export default theme
